[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "Break_Decimal"
version = "1.0.0"

readme = "README.md"
requires-python = ">=3.6"

[tool.hatch.build.targets.wheel]
packages = ["src"]